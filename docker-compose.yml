services:
  facialrec:
    build: .
    init: true
    environment:
      OPENCV_FFMPEG_CAPTURE_OPTIONS: rtsp_transport;tcp
      FACIALREC_REQUIRE_GPU: "0"
    volumes:
      - ./config:/app/config
      - ./db:/app/db
      - ./weights:/root/.deepface/weights
    restart: unless-stopped
